import styled from "styled-components";

export const Field = styled.input`
    
    margin-bottom: 2px;

    width: auto;
    height: 36px;

    border: 1.8px solid var(--verde);
    border-radius: 5px;

    color: var(--mostarda);
 

`;